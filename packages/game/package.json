{
  "name": "@chillbuilder/game",
  "private": true,
  "version": "0.1.0",
  "description": "ChillBuilder — the core game, built with Phaser + TypeScript + Vite.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.3.1",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "phaser": "4.2.1"
  }
}
