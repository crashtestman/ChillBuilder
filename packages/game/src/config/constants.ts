// Landscape base logical resolution. Phaser's Scale Manager (FIT) letterboxes
// this to fit any device/browser window without distorting the isometric grid.
export const GAME_WIDTH = 1280;
export const GAME_HEIGHT = 720;

// Placeholder ground plane size for M1. Real per-map dimensions arrive with
// the map def data (mapDefs/slice01.ts) in a later milestone.
export const GRID_COLS = 16;
export const GRID_ROWS = 16;
